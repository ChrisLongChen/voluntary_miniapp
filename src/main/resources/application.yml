spring:
  datasource:
    url: jdbc:mysql://localhost:3306/voluntary?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: "Asia/Shanghai"

mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 3307
